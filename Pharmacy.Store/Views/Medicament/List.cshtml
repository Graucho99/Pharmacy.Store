@model MedicamentListViewModel

<h1>@Model.CurrentCategory</h1>

<div class="row row-cols-1 row-cols-md-3 g-4">

    @foreach (var medicament in Model.Medicaments)
    {
       <partial name="_MedicamentCard" model="medicament" />
    }

</div>