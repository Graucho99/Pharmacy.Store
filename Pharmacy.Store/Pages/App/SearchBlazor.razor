@page "/app/search"
@page "/app"

<h3>Search medicaments</h3>


<div class="text-center bg-blue-100">
    <input class="border-4 w-1/3 rounded m-6 p-6 h-8
               border-blue-300" @bind-value="SearchText"
           @bind-value:event="oninput" placeholder="Search by medicament name" @onkeyup="Search" />
</div>


@if (!FilteredMedicaments.Any())
{
    <p>Nothing to show, sorry...</p>
    @if (SearchText.Length < 3)
    {
        <p>Make sure to enter at least 3 characters to search!</p>
    }
}
else
{
    <div class="p-2 grid grid-cols-1 sm:grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-3">
        @foreach (var medicament in FilteredMedicaments)
        {
            @* <div class="col-sm-4 col-lg-4 col-md-4">
                <div class="thumbnail">
                <img src="@medicament.ImageThumbnailUrl" alt="">
                <div class="caption">
                <h3 class="pull-right">@medicament.Price.ToString("c")</h3>
                <h3>
                <a href="/medicament/details/@medicament.MedicamentId">@medicament.Name</a>
                </h3>
                <p>@medicament.ShortDescription</p>
                </div>

                </div>
                </div>*@
            <MedicamentCard Medicament=@medicament></MedicamentCard>
        }
    </div>
}

